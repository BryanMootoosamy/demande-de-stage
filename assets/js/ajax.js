class Ajax {
    constructor(url, type, sync) {
        this.url = url;
        this.type = type;
        this.sync = sync;
    }
    newRequest(){
        let req = new XMLHttpRequest();
        
    }
}